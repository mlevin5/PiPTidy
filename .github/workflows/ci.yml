name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - run: brew install swiftlint swiftformat
      - run: make lint
      - run: make test
